

if __name__=="__main__":
    #ascii value starts from 97 to 123, so input 1 means show chr(97) so the formula will be chr(input+96)
    n=int(input())
    #print(chr(10+96))
    #this one requires join knowledge once I get that will work on it
